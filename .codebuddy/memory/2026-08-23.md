# 2026-08-23

## AGENTS.md 改进已应用
仓库已有 AGENTS.md，按要求未创建 CODEBUDDY.md，而是补充了 AGENTS.md：
- 新增 `## Commands — pure-PHP agents` 小节（agent-workerman composer install/start、tests 各命令与前置条件、agentRaw 命令）。
- 新增 `## agent-workerman — dual-connection model` 小节：每 WS 连接一个 WsHandler（$conn->context）、dump 长连接 + query 惰性连接分离、AsyncClient 状态机及修正的 TypePHP 两处协议缺陷（auth-plugin-data-part-2 偏移、CLIENT_CONNECT_WITH_DB NUL 结尾）。
- Frontend 小节补充 `src/lib/ws.ts`、`src/context/AppContext.tsx` + `src/hooks/useTraceRun.ts`。

## 前端 UI 改动
- 变更列表筛选栏（ResultPage `.filter-bar`）改为单行：`flex-wrap: nowrap` + `overflow-x: auto`；pills/select/match 加 `flex: 0 0 auto`，match 加 `white-space: nowrap`。
- TracePage 拉取按钮：collecting 时只显示 loading 图标（不再渲染 Play + 文案）。
- TracePage 进度区：`已拉取 N 条变更`（useTraceRun 新增 pulledCount，demo 只统计 DML 事件 30/31/32）+ `预估 P%` + `已耗时` + `取消拉取`。
- useTraceRun 真实模式预估百分比：按窗口内最新变更时间戳位置估算 `(latestTs-startMs)/(endMs-startMs)`，clamp 0~99。
- 验证：`npm run type-check` 与 `npm run build` 均通过；dev server 已在 127.0.0.1:5173 预览。

## 补充修复：变更列表筛选栏单行 + 无滚动条
- 用户反馈筛选条件仍不在一行且最后一个多选下拉显示不全/出现滚动条。
- 调整 `.filter-bar`：移除 `overflow-x: auto`（消除滚动条），`gap` 改为 `--spacing-xs`；`.filter-bar > * { min-width: 0; }` 允许子项收缩。
- `.select-sm` 改为 `flex: 0 1 auto`（可收缩）+ `max-width: 160px`，并在 `.filter-bar .select-sm` 加 `min-width: 80px` 防止过窄。
- 再次 `npm run type-check` + `npm run build` 通过；预览已刷新。
